/*
	Настройки выгрузки на сервер по SFTP (gulp/tasks/deploy.js).
	Доступы к серверу хранятся отдельно — в файле .env (см. .env.example), в git он не попадает.

	targets — список источник/приёмник. Для каждого:
	local  — локальная папка относительно корня проекта
	remote — папка на сервере (относительный путь считается от домашней директории SSH-пользователя)
	files  — какие файлы выгружать (glob относительно local)
	ignore — какие файлы не выгружать (glob относительно local)

	С флагом --delete на сервере удаляются файлы внутри remote, подходящие под files и не попадающие под ignore,
	которых нет локально. Перед первым запуском с --delete проверьте список через --dry-run.

	Ниже — пример для выгрузки собранного билда. Под конкретный проект (свой путь на сервере,
	дополнительные источники — например php/tpl-шаблоны отдельного бэкенда) поправьте этот файл.
*/
export default {
	targets: [
		{
			local: 'build',
			remote: 'www/example.ru/public_html',
			files: ['**/*'],
			ignore: ['**/*.map'],
		},
	],
};
